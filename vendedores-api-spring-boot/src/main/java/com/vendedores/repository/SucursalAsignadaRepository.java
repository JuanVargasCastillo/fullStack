package com.vendedores.repository;

import com.vendedores.models.SucursalAsignada;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SucursalAsignadaRepository extends JpaRepository<SucursalAsignada, Integer> {
}
